name: Build Halo AI Assistant Plugin

on:
  push:
    branches: [ main, master ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      # 构建前用备份覆盖 AiAssistantTab.vue，避免网页编辑/编码导致解析失败
      - name: Use fallback AiAssistantTab.vue
        run: |
          if [ -f .github/fallback/AiAssistantTab.vue.txt ]; then
            cp .github/fallback/AiAssistantTab.vue.txt ui/src/views/AiAssistantTab.vue
            echo "Applied fallback AiAssistantTab.vue"
          fi

      - name: Install UI deps
        working-directory: ui
        run: npm install

      # 在 GitHub runner 上安装并配置 Gradle（不需要仓库里有 gradlew）
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: 8.7

      - name: Build plugin with Gradle
        run: gradle clean build --no-daemon

      - name: Upload JAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: ai-assistant-plugin-jar
          path: build/libs/*.jar
